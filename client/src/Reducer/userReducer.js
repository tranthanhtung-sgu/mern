

export const userReducer=(state,action)=>{
    switch(action.type){
        case "RUNNING":
            return state
        case "SWIMMING":
            return state
        default:
            return "hhi"
    }
}
export default userReducer
